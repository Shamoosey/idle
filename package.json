{
  "name": "hotdog idle",
  "version": "1.0.0",
  "description": "hotdog idle game",
  "main": "index.js",
  "scripts": {
    "build": "tsc && cpx index.html dist && cpx styles.css dist"
  },
  "author": "sosler",
  "devDependencies": {
    "cpx": "^1.5.0",
    "typescript": "^5.8.3"
  }
}
